name: flash_cards_app
description: Flash Cards App for Baccalaureate Students
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0

  # State Management
  provider: ^6.1.1

  # Device Info
  device_info_plus: ^9.1.1

  # UI
  flutter_card_swiper: ^6.0.0
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  lottie: ^3.0.0
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  animations: ^2.0.11

  # Utils
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  uuid: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/animations/
    - assets/icons/
